import React from 'react';
import classes from './Nav.module.css';

export const Nav = () => {

    return (
        
        <div className={ classes.Nav }>
            <h1>Simple Tree</h1>
        </div>
                
    )
}